import styled from 'styled-components';

const StepCounter = styled.div`
  flex-grow: 0;
  margin-bottom: 24px;
  font-size: 13px;
  line-height: 1.38463;
  font-weight: 400;
`;

export default StepCounter;
